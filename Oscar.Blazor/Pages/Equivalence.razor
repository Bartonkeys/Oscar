@page "/equivalence"

@using Oscar.Blazor.Library.Components.Equivalence
@inject IMediator  Mediator
@inject NavigationManager NavigationManager
@inject IDialogService DialogService

<PageTitle>Registration Batches</PageTitle>

<MudDrawer @bind-Open="@openEquivalenceCreate" Height="100%" Width="50%" Anchor="@Anchor.End" Elevation="1" Variant="@DrawerVariant.Temporary">
    <CreateEquivalenceForm OnSubmit="@Refresh" Title="Create Equivalence"></CreateEquivalenceForm>
</MudDrawer>

<MudGrid>
    <MudItem xs="6">
        <MudButton OnClick="@OpenEquivalenceCreate" Variant="Variant.Filled" Color="Color.Primary">Create Equivalence</MudButton>
    </MudItem>
    <MudItem xs="6">
    </MudItem>
    <MudItem xs="12">
        <MudTable Dense="true" ServerData="@(new Func<TableState, Task<TableData<EquivalenceRequestDto>>>(ServerReload))" Hover="true" @ref="table" Loading="@_loading">
            <ToolBarContent>
                <MudText Typo="Typo.h6">Equivalence Uploads</MudText>
                <MudSpacer />
                <MudTextField T="string" ValueChanged="@(OnSearch)" Placeholder="Search" Adornment="Adornment.Start"
                              AdornmentIcon="@Icons.Material.Filled.Search" IconSize="Size.Medium" Class="mt-0"></MudTextField>
            </ToolBarContent>
            <HeaderContent>
                <MudTh>Id</MudTh>
                <MudTh>Status</MudTh>
                <MudTh>Requested By</MudTh>
                <MudTh>Filename</MudTh>
                <MudTh>Link</MudTh>
            </HeaderContent>
            <RowTemplate>
                <MudTd DataLabel="Id">@context.RequestID</MudTd>
                <MudTd DataLabel="Status">
                    <MudChip Label="true" Color="@GetColour(context.EquivalenceRequestStatus)" Size="Size.Small" Class="ml-auto mud-elevation-25">@context.EquivalenceRequestStatus.ToName()</MudChip>
                </MudTd>
                <MudTd DataLabel="Requested By">@context.RequestedBy</MudTd>
                <MudTd DataLabel="Filename">@context.FileName</MudTd>
                <MudTd DataLabel="Link">
                    @if (context.EquivalenceRequestStatus == EquivalenceRequestStatus.Processed)
                    {
                        <MudLink Href="@context.Url">
                            @if (!string.IsNullOrEmpty(context.Url))
                            {
                                <MudIcon Icon="@Icons.Material.Filled.Download" Color="Color.Primary" />
                            }
                        </MudLink>
                    }
                </MudTd>
            </RowTemplate>
            <NoRecordsContent>
                <MudText>No matching records found</MudText>
            </NoRecordsContent>
            <LoadingContent>
                <MudText>Loading...</MudText>
            </LoadingContent>
            <PagerContent>
                <MudTablePager />
            </PagerContent>
        </MudTable>
    </MudItem>
</MudGrid>

